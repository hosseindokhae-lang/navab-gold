(()=>{
const $=id=>document.getElementById(id);
const api=async(url,opt={})=>{opt.headers=Object.assign({'Content-Type':'application/json','Authorization':'Bearer '+(localStorage.getItem('navab_admin_token')||'')},opt.headers||{});const r=await fetch(url,opt);const j=await r.json().catch(()=>({}));if(!r.ok||j.ok===false)throw Error(j.error||'خطا در ذخیره اطلاعات');return j};
const ranges=[['r10_20','۱۰ تا ۲۰ میلیون'],['r20_30','۲۰ تا ۳۰ میلیون'],['r30_40','۳۰ تا ۴۰ میلیون'],['r40_50','۴۰ تا ۵۰ میلیون'],['r50_plus','۵۰ میلیون به بالا']];
const cats=[['ring','انگشتر'],['bracelet','دستبند'],['necklace','گردنبند'],['earring','گوشواره'],['pendant','آویز و پلاک'],['chain','زنجیر']];
let state={priceRanges:{},siteTitle:'',siteTagline:''};
async function read(k){const j=await api('/api/storage/'+k+'?t='+Date.now(),{headers:{'Cache-Control':'no-cache'}});let v=j.value;if(typeof v==='string'){try{v=JSON.parse(v)}catch{}}return v||{}}
async function write(k,v){return api('/api/storage/'+k,{method:'PUT',body:JSON.stringify({value:JSON.stringify(v)})})}
function notice(t,bad=false){let n=document.getElementById('siteSettingsNotice');if(!n)return;n.textContent=t;n.className='catalog-notice '+(bad?'bad':'ok');n.classList.remove('hidden');clearTimeout(n._t);n._t=setTimeout(()=>n.classList.add('hidden'),3500)}
function panel(){if($('siteSettingsManager')||!$('catalogManager'))return;const p=document.createElement('div');p.id='siteSettingsManager';p.className='panel';p.innerHTML=`<div class="ss-head"><div><strong>تنظیمات ویترین و متن سایت</strong><small>تغییرات واقعی روی سایت ذخیره می‌شوند.</small></div></div><div id="siteSettingsNotice" class="catalog-notice hidden"></div><div class="ss-section"><b>عکس بازه‌های قیمتی</b><div id="ssRanges" class="ss-grid"></div></div><div class="ss-section"><b>عکس دسته‌بندی‌ها</b><div id="ssCats" class="ss-grid"></div></div><div class="ss-section"><b>متن‌های اصلی سایت</b><div class="ss-text-grid"><label>عنوان اصلی<input id="ssTitle"></label><label>شعار / توضیح کوتاه<input id="ssTagline"></label></div><button id="ssTextSave" class="btn">ذخیره متن سایت</button></div>`;document.getElementById('catalogManager').after(p);render();$('ssTextSave').onclick=saveText}
function render(){const rg=$('ssRanges'),ct=$('ssCats');if(!rg)return;rg.innerHTML=ranges.map(([id,label])=>`<div class="ss-card"><div class="ss-label">${label}</div><div class="ss-preview">${state.priceRanges[id]?`<img src="${esc(state.priceRanges[id])}">`:'بدون عکس'}</div><label class="ss-upload">انتخاب / تغییر عکس<input type="file" accept="image/*" data-range="${id}"></label></div>`).join('');ct.innerHTML=cats.map(([id,label])=>`<div class="ss-card"><div class="ss-label">${label}</div><div class="ss-preview">${state.priceRanges['cat_'+id]?`<img src="${esc(state.priceRanges['cat_'+id])}">`:'بدون عکس'}</div><label class="ss-upload">انتخاب / تغییر عکس<input type="file" accept="image/*" data-cat="${id}"></label></div>`).join('');$('ssTitle').value=state.siteTitle||'';$('ssTagline').value=state.siteTagline||'';rg.querySelectorAll('[data-range]').forEach(e=>e.onchange=e=>saveImage(e.target.files?.[0],e.target.dataset.range));ct.querySelectorAll('[data-cat]').forEach(e=>e.onchange=e=>saveImage(e.target.files?.[0],'cat_'+e.target.dataset.cat))}
function esc(s){return String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]))}
function resize(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onerror=reject;r.onload=()=>{const i=new Image();i.onerror=reject;i.onload=()=>{const max=1000,s=Math.min(1,max/Math.max(i.width,i.height)),c=document.createElement('canvas');c.width=Math.max(1,Math.round(i.width*s));c.height=Math.max(1,Math.round(i.height*s));c.getContext('2d').drawImage(i,0,0,c.width,c.height);resolve(c.toDataURL('image/jpeg',.82))};i.src=r.result};r.readAsDataURL(file)})}
async function saveImage(file,key){if(!file)return;try{state.priceRanges[key]=await resize(file);await write('priceRanges',state.priceRanges);render();notice('عکس با موفقیت ذخیره شد و سایت آن را می‌خواند.')}catch(e){notice(e.message,true)}}
async function saveText(){try{state.siteTitle=$('ssTitle').value.trim();state.siteTagline=$('ssTagline').value.trim();await write('settings',{siteTitle:state.siteTitle,siteTagline:state.siteTagline});notice('متن سایت واقعاً ذخیره شد.')}catch(e){notice(e.message,true)}}
async function boot(){try{state.priceRanges=await read('priceRanges');const s=await read('settings');state.siteTitle=s.siteTitle||'';state.siteTagline=s.siteTagline||'';panel()}catch(e){console.error(e)}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,700));else setTimeout(boot,700);
})();